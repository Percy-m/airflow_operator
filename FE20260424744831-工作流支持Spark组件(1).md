# 工作流支持Spark组件

> 归属SF：SF20260421655859-Spark组件 | 归属微服务：AIPDWorkSpaceCoreService（日志文件下载接口）、AIPDWFSchedService（Airflow Operator部署）

---

## 方案设计

采用 5W2H 方法明确需求背景：

| 维度 | 说明 |
|------|------|
| **What** | 需要一个 Airflow 3.x Operator，用于对接异构离线算力防腐网关的 RESTful API，实现 Spark 等计算任务的生命周期管理。 |
| **Why** | AI 产品开发生产线选型使用 Airflow 3.x 编排工作流，部分节点需要 Spark 异构算力资源执行。用户通过 Python 定义 DAG，当前缺少标准化的 Spark 任务调度 Operator，导致 DAG 开发效率低、集成复杂。 |
| **Who** | DAG 开发者（数据工程师/算法工程师），需要方便地引用 Operator 定义和提交 Spark 任务。 |
| **Where** | 在 Airflow 3.x 工作流平台中，调用异构离线算力防腐网关的 RESTful API。 |
| **When** | 当需要为工作流增加 Spark 数据处理任务时，用户在 DAG 定义阶段使用该 Operator，在任务生命周期内依赖其功能。 |
| **How** | 实现独立的 `SparkOperator` 封装网关接口，支持任务提交（脚本、参数、环境变量、资源规格）、状态查询、日志获取、任务取消；采用 Deferrable 机制实现异步等待，释放 Worker 资源。 |
| **How much** | 开发自定义 SparkOperator、公共基类 BaseDataLakeOperator，与算力网关 REST API 对接。 |

---

## 方案分析和对比

### 2.1 Operator 实现方式

#### 方案 A：统一算力资源 AIDatalakeOperator
将所有计算资源（Spark、Ray 等）抽象为同一套接口，用户通过参数 `resource_type` 区分。Operator 内部根据类型拼接不同的网关接口路径或请求体，对外暴露一致的任务提交、查询、取消和日志接口。

#### 方案 B：独立类型 Operator（SparkOperator、RayOperator，推荐）
为每种计算引擎分别实现 Operator。每个 Operator 独立维护其与网关的交互逻辑，提供专用的参数校验和默认值。公共逻辑（HTTP 客户端、重试逻辑）抽离为基类。

**Spark 与 Ray API 差异分析：**
- 启动接口参数结构差异大：Spark 需资源规格配置，Ray 不需要
- Spark 通过接口响应获取日志，Ray 方式不同

#### 方案 C：原生 SparkSubmitOperator
使用 Airflow 社区原生 SparkSubmitOperator，直接调用 spark-submit 提交任务。

| 对比维度 | 方案A：统一 AIDatalakeOperator | 方案B：独立 Operator（推荐） | 方案C: 原生 SparkSubmitOperator |
|----------|-----------------------------------|----------------------|----------------------|
| **网关接口适配** | 接口差异巨大，统一操作符内部充斥条件分支和参数映射逻辑，复杂度高 | 每个Operator独立封装对应接口，参数映射清晰 | 无需适配网关，直接调用 spark-submit，与自定义网关不兼容 |
| **易用性** | 用户需根据资源类型传递不同结构的参数，参数校验困难，IDE提示差 | Operator名称和参数与计算引擎一一对应，IDE自动补全友好 | 熟悉 Spark 的用户直观，但依赖底层集群配置 |
| **扩展性** | 新增算力需修改核心参数结构和路由逻辑，影响已有功能 | 新增算力只需新增 Operator 类，影响范围隔离 | 专针对 Spark，无法扩展其他算力 |
| **维护性** | 接口变动可能影响所有资源，回归测试范围大 | 各 Operator 维护边界清晰，公共逻辑由基类复用 | 社区维护稳定，但自定义网关不适配 |
| **Airflow 生态习惯** | 社区 Operator 虽支持多种任务但接口结构高度一致，本方案差异远超统一封装范围 | 社区广泛采用独立 Operator 模式 | 社区提供但无法满足统一异构网关管理 |
| **风险** | 参数验证和请求构造逻辑指数级复杂化 | 公共逻辑需在基类维护，抽离不当可能重复 | 无法统一管理异构资源 |

**总结**：选择**方案 B（独立 SparkOperator + 公共基类）**。Spark 和 Ray 的 API 接口在 URL 路径、请求体结构、参数维度等各方面均存在本质差异，统一 Operator 不仅无法减少用户成本，反而因条件分支和复杂验证逻辑增加维护负担。独立方案让每个 Operator 与对应计算引擎 API 一一映射，符合 Airflow 社区惯例。公共逻辑提取为 `BaseAIDataLakeOperator` 基类，子类仅实现引擎特定逻辑。

### 2.2 数据输入输出方式

#### OBS 挂载到集群 + 本地路径直引方案
将 OBS 挂载到 Airflow 所在集群本地文件系统，Operator 直接操作本地路径。Spark 集群通过委托机制无认证访问 OBS，实现数据透明流转。

| 功能特性 | 说明 | 用户感知 |
|----------|------|----------|
| **路径透明** | OBS 挂载为本地文件系统，路径统一为本地格式 | 用户无需区分本地与对象存储 |
| **无感数据流转** | 挂载文件系统自动同步 | 脚本修改即时生效 |
| **Spark 集群委托访问** | 通过委托机制无认证访问 | 无需配置认证信息 |
| **输出自动注册** | 输出路径自动写入 XCom | 下游任务可直接获取 |

**路径转换策略：**

- 转换规则：`/mnt/OBS/bucket/path` → `OBS://bucket/path`
- 识别范围：仅 Operator 参数值，不解析 Spark 脚本内容
- 转换时机：execute 阶段提交前自动完成，默认启用

### 2.3 接口调用认证方案

#### Connection 存储 Token

用户在 Airflow Connection 中配置算力网关的认证 Token，Operator 执行时从 Connection 中读取凭证并直接调用网关接口。

| 功能特性 | 说明 |
|----------|------|
| **认证方式** | 通过 Airflow Connection 存储网关访问 Token |
| **配置方式** | 用户在 Airflow UI 中创建 Connection，填写 Token 和网关地址 |
| **凭证读取** | Operator 在 execute 方法中通过 Hook 读取 Connection 配置 |
| **Token 管理** | 用户负责 Token 的更新和轮换 |

**实施说明**：当前阶段暂不考虑 Token 隔离，直接使用 Connection 方式读取秘钥，简化实现复杂度。后续根据安全需求可评估引入内部 API 服务代理方案。

---

## 用例分析

### UC001：提交 Spark 计算任务

| 字段 | 内容 |
|------|------|
| **用例编号** | UC001 |
| **用例名称** | 提交 Spark 计算任务 |
| **用例描述** | DAG 开发者在 DAG 定义脚本中引用 SparkOperator，将数据处理脚本路径及依赖配置提交至算力网关，启动一个 Spark 计算任务，并根据 deferrable 参数决定采用异步等待或同步阻塞模式。 |
| **主要参与者** | DAG 开发者 |
| **前置条件** | - 用户 DAG 脚本定义正确<br>- 网关服务可用<br>- 用户拥有目标项目的 DAG 创建、任务提交权限<br>- OBS 已挂载到 Operator 所在本地文件系统 |
| **后置条件/验收条件** | 系统成功向网关发起任务创建请求，返回有效的任务 ID；轮询任务状态至任务结束；若请求失败，返回可读的错误信息，任务实例失败。 |
| **基本操作流程** | 1. 用户在 DAG 文件中实例化 SparkOperator，填入参数：主脚本路径、依赖文件列表、执行参数、环境变量、资源规格、资源 ID 等<br>2. DAG 调度运行到该任务，Operator 的 execute 方法执行：读取本地脚本和依赖，自动路径转换，从 Connection 读取认证凭证，向算力网关提交任务请求，网关返回任务 ID 后写入 XCom。 |
| **分支/异常流程** | **1a.** 用户定义的 Operator 参数校验不通过<br>&emsp;1a1. 任务状态置为错误，日志中记录错误信息<br><br>**2a.** 提交超时或网络错误<br>&emsp;2a1. 系统按重试策略重试，超过最大次数后标记失败并记录日志<br><br>**2b.** 网关返回资源 ID 或规格不合法<br>&emsp;2b1. 系统抛错停止执行，记录日志<br><br>**2c.** 系统轮询连续失败达 10 次<br>&emsp;2c1. 系统抛出异常，释放资源，任务标记失败 |
| **输入** | DAG 定义脚本，包含任务类型、脚本路径、依赖文件、运行参数、资源规格等信息 |
| **输出** | 任务 ID 写入 XCom；任务状态实时变化；脚本执行输出内容由 Spark 任务实际输出决定 |
| **规则说明** | - 参数校验：资源类型、资源规格需校验，失败抛异常并记录错误提示<br>- 输出路径自动提取：OBS 挂载路径自动转换后通过环境变量或参数传递给 Spark 任务<br>- Trigger 每次轮询成功后更新最新状态和日志链接到 XCom |
| **非功能要求** | - 可靠性：提交失败后 Airflow 提供任务级重试能力<br>- 资源效率：Deferrable 模式下 Worker 线程释放，仅 Trigger 进程低开销轮询 |

### UC002：查看 Spark 计算任务状态

| 字段 | 内容 |
|------|------|
| **用例编号** | UC002 |
| **用例名称** | 查看 Spark 计算任务状态 |
| **用例描述** | Operator 提交任务后在独立进程中异步轮询网关状态，用户通过页面实时查看任务执行状态，全程无需定义额外 Sensor 节点。 |
| **主要参与者** | DAG 开发者 |
| **前置条件** | SparkOperator 已成功提交任务；网关可用。 |
| **后置条件/验收条件** | 系统检测到任务终态（成功/失败/取消）后 Airflow 任务状态更新；若任务不存在或轮询超时返回相应异常。 |
| **基本操作流程** | 1. 用户在 Airflow UI 查看任务状态<br>2. UI 展示任务实时状态（状态由系统自动轮询并写入 XCom）<br>3. 用户可在 DAG 中查询状态历史 |
| **分支/异常流程** | **1a.** 系统轮询网关返回任务不存在或终态失败<br>&emsp;1a1. XCom 更新终态，UI 同步显示任务失败<br><br>**1b.** 系统轮询连续超时或网络异常达 10 次<br>&emsp;1b1. 系统触发熔断，任务标记失败，UI 显示失败状态<br><br>**2a.** 用户在 UI 中终止任务<br>&emsp;2a1. 系统向网关发送取消请求，请求失败后任务标记失败退出 |
| **输入** | DAG 运行实例、任务 ID |
| **输出** | 任务实时状态写入 XCom，Airflow UI 展示最新状态 |
| **规则说明** | - 轮询间隔默认 30s，最小区间不低于 10s<br>- 每次轮询成功后自动更新状态到 XCom 和任务实例 |
| **非功能要求** | - 性能：至多 30s 检测一次，避免频繁请求网关<br>- 可靠性：连续超时失败时系统主动调用停止接口结束任务<br>- 资源效率：Trigger 独立进程运行，不占用 Worker 资源 |

### UC003：获取 Spark 计算任务日志

| 字段 | 内容 |
|------|------|
| **用例编号** | UC003 |
| **用例名称** | 获取 Spark 计算任务日志 |
| **用例描述** | 用户在任务执行中或结束后，通过 Airflow Web UI 获取 Spark 任务的日志下载链接，以便排查问题或记录。 |
| **主要参与者** | DAG 开发者 |
| **前置条件** | 任务已提交且任务 ID 有效；Spark 任务存在有效的日志输出；日志文件存储在网关可访问的位置。 |
| **后置条件/验收条件** | Web UI 任务日志页签中周期性展示 Spark 日志下载链接，下载压缩包后包含有效的 Spark 日志内容。日志保留策略为 1 个月。 |
| **基本操作流程** | 1. 用户在 Airflow UI 日志页签查看日志<br>2. UI 展示日志下载链接，用户点击链接下载日志压缩包 |
| **分支/异常流程** | **1a.** 系统轮询获取日志接口返回空或不可用<br>&emsp;1a1. 页面提示"日志生成中"或"暂不可用"<br><br>**1b.** 日志文件大小超过 1GB 限制<br>&emsp;1b1. 页面提示"日志文件过大，无法提供下载" |
| **输入** | 任务 ID、任务信息 |
| **输出** | 日志页签周期性输出 Spark 日志下载链接 |
| **规则说明** | - 日志链接刷新频率与状态轮询频率保持一致<br>- 日志保留期限为 1 个月，过期后链接失效<br>- 日志下载文件大小限制为 1GB，超过该限制则不提供下载链接并提示 |
| **非功能要求** | 不涉及，日志仅涉及展示下载链接，不存储或转发文件内容。 |

### UC004：取消 Spark 计算任务

| 字段 | 内容 |
|------|------|
| **用例编号** | UC004 |
| **用例名称** | 取消 Spark 计算任务 |
| **用例描述** | 当用户需要终止一个正在运行的 Spark 任务时，通过 Web UI 标记任务状态为失败，系统向网关发送取消指令。 |
| **主要参与者** | DAG 开发者 |
| **前置条件** | 任务 ID 有效且任务当前处于可取消状态（PENDING、QUEUED、RUNNING）。 |
| **后置条件/验收条件** | 网关接受取消请求，任务最终状态变为失败或已取消。 |
| **基本操作流程** | 1. 用户在网页中标记任务状态为失败<br>2. 系统向网关发送停止请求，记录取消确认，任务状态置为失败<br>3. 用户在网页看到任务为取消或失败状态 |
| **分支/异常流程** | **2a.** 任务已完成无法取消<br>&emsp;2a1. 系统记录信息，不做错误处理（幂等）<br><br>**2b.** 网关未响应取消请求<br>&emsp;2b1. 系统重试后超时，记录错误日志，状态刷新为失败 |
| **输入** | 任务 ID |
| **输出** | 任务停止，任务状态为失败或已取消 |
| **规则说明** | - 仅可取消 PENDING、QUEUED、RUNNING 状态的任务<br>- 已终态任务取消操作幂等处理 |
| **非功能要求** | 不涉及，取消操作为低频操作，无并发或性能风险。 |

---

## 可靠性属性分析

| 场景 | 设计与措施 |
|------|-------------|
| **高并发场景** | 不涉及，Operator 生命周期在 DAG 运行实例内，调度由 Airflow 控制 |
| **分布式事务** | 不涉及，无跨系统事务要求 |
| **网关不可用降级** | 触发重试机制，重试耗尽后取消远程作业并标记本地任务失败 |
| **OBS 挂载不可用** | 无法读取脚本直接标记任务失败 |
| **状态轮询限流** | 轮询间隔限制 30s，避免过度请求外部系统 |
| **连续失败熔断** | 连续 10 次状态查询失败触发熔断，停止轮询执行清理 |

## 安全属性分析

| 安全维度 | 设计与措施 |
|----------|-------------|
| **认证方式** | Operator 通过 Airflow Connection 存储的 Token 访问算力网关 |
| **日志安全** | 日志下载链接通过网关提供，保留 1 个月，不涉及敏感数据外泄 |

## 隐私分析

不涉及，系统不收集个人信息。

---

## US 定义

| US编号 | US名称 | US描述 | 关联用例及场景 | 前后端协同契约 | 优先级 |
|--------|--------|--------|----------------|----------------|--------|
| US20260507927784 | 实现 Spark 任务提交核心逻辑 | 【As】作为一个 DAG 开发者<br>【I want】我想要在 DAG 脚本中引用 SparkOperator 并提交 Spark 任务，系统自动完成参数校验和路径转换<br>【so that】以便于无需手动处理协议转换即可使用 OBS 挂载路径<br><br>【given】DAG 中已配置脚本路径、资源规格、运行参数；Airflow Connection 已配置网关认证凭证<br>【When】DAG 调度执行到该任务节点<br>【Then】若参数校验失败，任务标记错误并记录日志；若校验通过，从 Connection 读取凭证，直接向算力网关提交任务并获取 job_id 写入 XCom，触发 Deferrable 挂起协程 | UC001 (基本流程)、UC001 (分支 1a、2a、2b) | **Operator 侧**：实现 SparkOperator 继承 BaseDataLakeOperator，支持 OBS 路径转换、参数校验、Connection 凭证读取、任务提交逻辑 | P0 |
| US20260507927991 | 实现 Spark 任务异步状态轮询 | 【As】作为一个 DAG 开发者<br>【I want】我想要在任务提交后释放 Worker 资源，系统自动轮询状态直到终态<br>【so that】以便于长时间运行的任务不阻塞 Worker 池<br><br>【given】任务已提交并获取 job_id；Airflow Connection 已配置网关认证凭证<br>【When】Trigger 按间隔轮询任务状态<br>【Then】轮询成功时更新状态到 XCom；达到终态时触发回调恢复 Operator，同步 Airflow 任务状态；连续失败达阈值时触发熔断，任务标记失败 | UC002 (基本流程)、UC002 (分支 2a、2b、2c) | **Operator 侧**：实现 Deferrable 异步轮询机制、状态管理、熔断清理逻辑 | P0 |
| US20260507928041 | 实现 Spark 任务日志链接获取 | 【As】作为一个 DAG 开发者<br>【I want】我想要通过 Airflow UI 查看 Spark 任务的日志下载链接<br>【so that】以便于排查问题时无需登录远程集群<br><br>【given】任务正在运行或已结束；Airflow Connection 已配置网关认证凭证<br>【When】轮询状态时同步获取日志链接<br>【Then】日志链接在 Airflow 任务日志页签展示，用户点击可下载；若日志接口返回空则页签显示"日志生成中"；若日志文件超过 1GB 则页面显示"日志文件过大，无法提供下载" | UC003 (基本流程)、UC003 (分支 2a、2b) | **Operator 侧**：在轮询状态时同步获取日志链接并更新 XCom | P0 |
| US20260507927993 | 实现 Spark 任务取消功能 | 【As】作为一个 DAG 开发者<br>【I want】我想要通过 UI 取消正在运行的任务<br>【so that】以便于避免无效任务继续消耗算力资源<br><br>【given】任务处于可取消状态；Airflow Connection 已配置网关认证凭证<br>【When】用户在 UI 标记任务失败请求取消<br>【Then】系统向网关发送取消指令，任务状态更新为失败；若任务已终态，静默处理不报错 | UC004 (基本流程)、UC004 (分支 2a、2b) | **Operator 侧**：实现任务取消逻辑与幂等处理 | P0 |
