# 工作流支持Ray组件

> 归属SF：SF20260421655713-Ray组件 | 归属前端微服务：不涉及 | 归属后端微服务：AIPDWFSchedService（Airflow Operator部署）

---

## 方案设计

采用 5W2H 方法明确需求背景：

| 维度 | 说明 |
|------|------|
| **What** | 需要一个 Airflow 3.x Operator，用于对接异构离线算力防腐网关的 RESTful API，实现 Ray 计算任务的生命周期管理（提交、状态查询、取消）。日志获取功能不在本版本范围。 |
| **Why** | AI 产品开发生产线选型使用 Airflow 3.x 编排工作流，机器学习训练、强化学习、分布式 Python 计算等场景需要 Ray 异构算力资源执行。用户通过 Python 定义 DAG，当前缺少标准化的 Ray 任务调度 Operator，导致 DAG 开发效率低、集成复杂。 |
| **Who** | DAG 开发者（数据工程师/算法工程师/ML 工程师），需要方便地引用 Operator 定义和提交 Ray 任务。 |
| **Where** | 在 Airflow 3.x 工作流平台中，调用异构离线算力防腐网关的 RESTful API。 |
| **When** | 当需要为工作流增加 Ray 分布式计算任务（如分布式训练、超参搜索、批量推理）时，用户在 DAG 定义阶段使用该 Operator，在任务生命周期内依赖其功能。 |
| **How** | 实现独立的 `RayOperator` 封装网关接口，支持任务提交（entrypoint、runtime_env、任务级资源 CPU/GPU/Memory）、状态查询、任务取消；采用 Deferrable 机制实现异步等待，释放 Worker 资源。 |
| **How much** | 开发自定义 RayOperator，复用已有的公共基类 `BaseAIDataLakeOperator`（与 SparkOperator 共享 HTTP 客户端、重试逻辑、Connection 认证、OBS 路径转换），仅新增 Ray 引擎特定的参数构造与状态解析逻辑。 |

---

## 方案分析和对比

### 2.1 Operator 实现方式

#### 方案 A：统一算力资源 AIDatalakeOperator
将所有计算资源（Spark、Ray 等）抽象为同一套接口，用户通过参数 `resource_type` 区分。Operator 内部根据类型拼接不同的网关接口路径或请求体，对外暴露一致的任务提交、查询、取消和日志接口。

#### 方案 B：独立类型 Operator（RayOperator，与已有 SparkOperator 平行，推荐）
为每种计算引擎分别实现 Operator。RayOperator 独立维护其与网关的交互逻辑，提供专用的参数校验和默认值。公共逻辑（HTTP 客户端、重试逻辑、OBS 路径转换、Connection 认证）由基类 `BaseAIDataLakeOperator` 复用。

**Ray 与 Spark API 本质差异分析：**
- **资源模型不同**：Spark 区分 driver/executor 节点角色，需配置 `driverResourceSpec`/`executorResourceSpec`/`executorNumber`；Ray 采用任务级资源模型，仅需 `entrypointNumCpus`/`entrypointNumGpus`/`entrypointMemory`。
- **入参结构不同**：Spark 用 `mainPythonFile + mainArgs + dependencyPyFiles` 描述脚本入口；Ray 用 `entrypoint`（一条 shell 命令）+ `runtimeEnv`（声明工作目录与依赖）。
- **状态机不同**：Spark 状态枚举包含 `QUEUED`、`QUEUED_TIMEOUT`、`RUNNING_TIMEOUT`、`CANCELING`、`CANCELED` 等过渡态；Ray 原生状态仅 `PENDING/RUNNING/SUCCEEDED/FAILED/STOPPED`，无排队态、无超时细分、取消直接落 `STOPPED` 终态。
- **依赖管理方式不同**：Spark 通过显式列出依赖 `.zip` 文件；Ray 通过 `runtimeEnv` 声明 pip/conda/working_dir，依赖管理由 Ray 运行时负责。

#### 方案 C：原生 RayJobSubmissionClient
使用 Ray 社区原生的 `JobSubmissionClient` SDK，直接调用 Ray 集群的 Jobs API（`http://<head-node>:8265/api/jobs/`）提交任务。

| 对比维度 | 方案A：统一 AIDatalakeOperator | 方案B：独立 RayOperator（推荐） | 方案C：原生 RayJobSubmissionClient |
|----------|--------------------------------|--------------------------------|--------------------------------------|
| **网关接口适配** | Spark 与 Ray 在路径、请求体、参数维度上差异巨大，统一 Operator 内部充斥条件分支与参数映射逻辑，复杂度高 | RayOperator 独立封装 `/ray-job/v1/*` 接口，参数映射清晰，无 if/else 分支污染 | 绕开算力网关直连 Ray 集群，与本应用统一鉴权、统一调度、统一日志归集体系不兼容 |
| **易用性** | 用户需根据资源类型传递不同结构的参数，IDE 自动补全失效，参数校验复杂 | `RayOperator(entrypoint=..., runtime_env=..., entrypoint_num_cpus=...)` 与 Ray 原生概念一一对应，IDE 提示友好 | 熟悉 Ray 的用户直观，但需要用户自行管理集群地址、网络可达性、认证凭证 |
| **扩展性** | 新增算力需修改核心参数结构和路由逻辑，回归影响面大 | 新增算力（如 Flink、Dask）只需新增 Operator 类，影响范围隔离 | 仅针对 Ray 单一引擎，无法扩展其他算力 |
| **维护性** | 接口变动可能影响所有资源，回归测试范围大 | Operator 维护边界清晰，公共逻辑由 `BaseAIDataLakeOperator` 基类复用 | 受 Ray 社区版本演进影响，且无法与算力网关侧的资源配额、计费、审计打通 |
| **Airflow 生态习惯** | 社区 Operator 虽支持多任务但接口结构高度一致，本方案差异远超统一封装能容纳的范围 | 社区广泛采用独立 Operator 模式（如 `SparkSubmitOperator`、`KubernetesPodOperator`） | 仅适合直连 Ray 集群的场景，不适配本应用的网关代理模式 |
| **风险** | 参数验证和请求构造逻辑指数级复杂化 | 公共逻辑需在基类维护，抽离不当可能重复——但 SparkOperator 已经验证基类设计可行 | 跨过网关后无法纳入统一资源管控，违反系统架构边界 |

**总结**：选择**方案 B（独立 RayOperator + 复用公共基类 BaseAIDataLakeOperator）**。Ray 与 Spark 在资源模型、入参结构、状态机、依赖管理上均存在本质差异，统一 Operator 不可行；而绕开网关直连 Ray 集群破坏架构边界。独立 Operator 让每个引擎与对应网关 API 一一映射，基类复用降低重复成本，符合 Airflow 社区惯例，也与已上线的 SparkOperator 保持架构一致。

### 2.2 数据输入输出方式

#### OBS 挂载到集群 + 本地路径直引方案（沿用 Spark 既有方案）
将 OBS 挂载到 Airflow 所在集群本地文件系统，Operator 直接操作本地路径。Ray 集群通过委托机制无认证访问 OBS，实现数据透明流转。

| 功能特性 | 说明 | 用户感知 |
|----------|------|----------|
| **路径透明** | OBS 挂载为本地文件系统，路径统一为本地格式 | 用户无需区分本地与对象存储 |
| **无感数据流转** | 挂载文件系统自动同步 | 脚本修改即时生效 |
| **Ray 集群委托访问** | 通过委托机制无认证访问 OBS | 无需配置认证信息 |
| **运行结果落 OBS** | Ray 任务输出经 `runtime_env.working_dir` 指向 OBS 路径 | 下游任务可直接读取 |

**路径转换策略：**

- 转换规则：`/mnt/OBS/bucket/path` → `OBS://bucket/path`
- **识别范围**：仅 Operator **结构化参数值**（如 `runtime_env.working_dir`、`runtime_env.py_modules` 中的路径项）；不解析 `entrypoint` 命令字符串内容，用户在 `entrypoint` 内部引用的路径需自行使用 `OBS://` 协议或确保 Ray 集群本地可读。
- 转换时机：execute 阶段提交前自动完成，默认启用，可通过 `convert_obs_path=False` 关闭。

### 2.3 接口调用认证方案

#### Connection 存储 Token（沿用 Spark 既有方案）

用户在 Airflow Connection 中配置算力网关的认证 Token，Operator 执行时从 Connection 中读取凭证并直接调用网关接口。

| 功能特性 | 说明 |
|----------|------|
| **认证方式** | 通过 Airflow Connection 存储网关访问 Token |
| **配置方式** | 用户在 Airflow UI 中创建 Connection，填写 Token 和网关地址 |
| **凭证读取** | RayOperator 在 execute 方法中通过 Hook 读取 Connection 配置 |
| **Token 管理** | 用户负责 Token 的更新和轮换 |

**实施说明**：当前阶段暂不考虑 Token 隔离，直接使用 Connection 方式读取秘钥，简化实现复杂度。后续根据安全需求可评估引入内部 API 服务代理方案。RayOperator 与 SparkOperator 共用同一套 Connection 类型，单一 Connection 同时承载 Spark 和 Ray 提交鉴权。

---

## 用例分析

### UC001：提交 Ray 计算任务

| 字段 | 内容 |
|------|------|
| **用例编号** | UC001 |
| **用例名称** | 提交 Ray 计算任务 |
| **用例描述** | DAG 开发者在 DAG 定义脚本中引用 RayOperator，将 entrypoint 命令、runtime_env 依赖描述与任务级资源（CPU/GPU/Memory）提交至算力网关，启动一个 Ray 计算任务，并采用 Deferrable 异步等待模式。 |
| **主要参与者** | DAG 开发者 |
| **前置条件** | - 用户 DAG 脚本定义正确<br>- 算力网关服务可用<br>- 用户拥有目标项目的 DAG 创建、任务提交权限<br>- OBS 已挂载到 Operator 所在本地文件系统<br>- Airflow Connection 已配置网关地址和 Token |
| **后置条件/验收条件** | 系统成功向网关发起任务创建请求，返回有效的 `job_id` 并写入 XCom；Trigger 在独立进程中开始轮询任务状态直至终态；若请求失败，返回可读的错误信息，任务实例标记失败。 |
| **基本操作流程** | 1. 用户在 DAG 文件中实例化 RayOperator，填入参数：entrypoint（如 `python train.py --epochs 10`）、runtime_env（working_dir/pip/env_vars）、entrypoint_num_cpus、entrypoint_num_gpus、entrypoint_memory、workspace_id 等<br>2. DAG 调度运行到该任务节点，Operator 的 execute 方法执行：读取本地 OBS 挂载路径中的脚本与依赖，自动将 `runtime_env.working_dir` 等结构化路径由 `/mnt/OBS/...` 转换为 `OBS://...`，从 Connection 读取认证凭证，向算力网关 `POST /ray-job/v1/create` 提交任务，网关返回 `job_id` 后写入 XCom<br>3. Operator 通过 Deferrable 机制挂起协程释放 Worker 资源，交由 Trigger 异步轮询。 |
| **分支/异常流程** | **1a.** 用户定义的 Operator 参数校验不通过（entrypoint 为空 / entrypoint_num_cpus 或 entrypoint_num_gpus 为负数 / workspace_id 缺失）<br>&emsp;1a1. Operator 不发起提交请求，任务状态置为错误，日志中记录字段级错误详情<br><br>**2a.** 提交超时或网络错误<br>&emsp;2a1. 系统按指数退避重试策略重试 3 次（1s/2s/4s），超过最大次数后标记任务失败并记录日志<br><br>**2b.** 网关返回 Ray 任务级资源不合法（如 CPU 配额超限、GPU 类型不可用）<br>&emsp;2b1. 系统抛错立即停止执行，不重试，记录日志<br><br>**2c.** OBS 挂载不可用导致 `runtime_env.working_dir` 转换后路径不可访问<br>&emsp;2c1. Operator 不发起提交请求，任务直接标记失败，日志记录路径不可用原因 |
| **输入** | DAG 定义脚本，包含 entrypoint 命令、runtime_env 字典、任务级资源规格、workspace_id、endpoint_name、ray_version 等信息 |
| **输出** | 任务 `job_id` 写入 XCom；任务状态实时同步至 XCom；脚本执行结果由 Ray 任务自身产出 |
| **规则说明** | - 参数校验：`entrypoint` 不可为空字符串；`entrypoint_num_cpus`、`entrypoint_num_gpus`、`entrypoint_memory` 必须为非负数<br>- 路径转换：仅对结构化字段（`runtime_env.working_dir`、`runtime_env.py_modules`）执行 `/mnt/OBS/...` → `OBS://...` 转换；`entrypoint` 命令字符串保持原样不解析<br>- 任务 `name` 字段默认使用 `{dag_id}-{task_id}-{run_id}` 规则拼接，便于网关侧排查 |
| **非功能要求** | - **可靠性**：提交失败后 Airflow 提供任务级重试能力<br>- **资源效率**：Deferrable 模式下 Worker 线程释放，仅 Trigger 进程低开销轮询<br>- **数据约束**：单次提交请求体大小不超过 50KB（含 `runtime_env` 内联依赖描述） |

---

### UC002：查看 Ray 计算任务状态

| 字段 | 内容 |
|------|------|
| **用例编号** | UC002 |
| **用例名称** | 查看 Ray 计算任务状态 |
| **用例描述** | RayOperator 提交任务后由 Trigger 在独立进程中异步轮询网关状态，用户通过 Airflow Web UI 实时查看任务执行状态，全程无需定义额外 Sensor 节点。 |
| **主要参与者** | DAG 开发者 |
| **前置条件** | RayOperator 已成功提交任务并获取 `job_id`；算力网关可用；Trigger 进程正常运行。 |
| **后置条件/验收条件** | 系统检测到任务终态（`SUCCEEDED`/`FAILED`/`STOPPED`）后 Airflow 任务状态同步更新；XCom 中 state 字段反映网关返回的最新值；若任务不存在或轮询连续失败达熔断阈值，任务标记失败并记录原因。 |
| **基本操作流程** | 1. 用户在 Airflow UI 查看任务状态<br>2. UI 展示任务实时状态（状态由 Trigger 按 30s 间隔自动轮询 `POST /ray-job/v1/query` 并写入 XCom）<br>3. Trigger 检测到 Ray 原生终态（`SUCCEEDED`/`FAILED`/`STOPPED`）时触发回调，恢复 Operator 执行，同步 Airflow 任务最终状态<br>4. 用户可在 DAG 中通过 XCom 查询状态历史 |
| **分支/异常流程** | **1a.** 系统轮询网关返回任务不存在（404）<br>&emsp;1a1. XCom 更新终态标记 FAILED，UI 同步显示任务失败<br><br>**1b.** 系统轮询连续超时或网络异常达 10 次<br>&emsp;1b1. 系统触发熔断，停止轮询执行清理，恢复 Operator 并标记任务失败，UI 显示失败状态<br><br>**2a.** 用户在 UI 中标记任务失败触发取消（详见 UC003） |
| **输入** | DAG 运行实例、任务 `job_id` |
| **输出** | 任务实时状态写入 XCom，Airflow UI 展示最新状态 |
| **规则说明** | - 轮询间隔默认 30s，最小区间不低于 10s（由 `poll_interval` 参数控制）<br>- 每次轮询成功后自动更新 state 到 XCom<br>- Ray 原生状态枚举映射：`PENDING/RUNNING` 视为运行中态，`SUCCEEDED` 视为成功终态，`FAILED/STOPPED` 视为失败终态 |
| **非功能要求** | - **性能**：至多 30s 检测一次，避免频繁请求网关<br>- **可靠性**：连续 10 次失败时系统主动调用取消接口结束任务，避免幽灵作业<br>- **资源效率**：Trigger 独立进程运行，不占用 Worker 资源 |

---

---

### UC003：取消 Ray 计算任务

| 字段 | 内容 |
|------|------|
| **用例编号** | UC003 |
| **用例名称** | 取消 Ray 计算任务 |
| **用例描述** | 当用户需要终止一个正在运行的 Ray 任务时，通过 Airflow Web UI 标记任务状态为失败，系统向网关发送取消指令，Ray 侧任务状态直接落 `STOPPED` 终态（无 `CANCELING` 中间态）。 |
| **主要参与者** | DAG 开发者 |
| **前置条件** | `job_id` 有效且任务当前处于可取消状态（`PENDING` 或 `RUNNING`）。 |
| **后置条件/验收条件** | 网关接受取消请求，Ray 任务最终状态变为 `STOPPED`，Airflow 侧任务状态置为失败。 |
| **基本操作流程** | 1. 用户在 Airflow UI 标记任务状态为失败<br>2. Operator `on_kill` 钩子触发，向网关 `POST /ray-job/v1/cancel` 发送停止请求<br>3. 网关接收后将 Ray 任务推至 `STOPPED` 终态，Trigger 下一轮询获取到终态后恢复 Operator 并更新 Airflow 任务状态 |
| **分支/异常流程** | **2a.** 任务已终态（`SUCCEEDED`/`FAILED`/`STOPPED`）时收到取消请求<br>&emsp;2a1. 网关幂等处理返回成功，Operator 不做错误处理，仅记录日志<br><br>**2b.** 网关未响应取消请求<br>&emsp;2b1. 系统按重试策略重试 2 次，超过重试次数后记录错误日志，任务状态强制刷新为失败 |
| **输入** | 任务 `job_id`、`workspace_id` |
| **输出** | 任务停止，Ray 任务状态为 `STOPPED`，Airflow 任务状态为 failed |
| **规则说明** | - 仅可取消 `PENDING`、`RUNNING` 状态的任务（Ray 原生无 `QUEUED` 中间态）<br>- 已终态任务取消操作幂等处理<br>- Ray 取消为同步操作，无 `CANCELING` 过渡态，与 Spark 流程不同 |
| **非功能要求** | 不涉及，取消操作为低频操作，无并发或性能风险。 |

---

## 可靠性属性分析

| 场景 | 设计与措施 |
|------|-------------|
| **高并发场景** | 不涉及，Operator 生命周期在 DAG 运行实例内，调度由 Airflow 控制 |
| **分布式事务** | 不涉及，无跨系统事务要求 |
| **网关不可用降级** | 提交阶段触发指数退避重试 3 次，重试耗尽后取消远程作业并标记本地任务失败 |
| **OBS 挂载不可用** | 无法读取 `runtime_env.working_dir` 指向路径时直接标记任务失败，不发起提交 |
| **状态轮询限流** | 轮询间隔默认 30s，最低不低于 10s，避免过度请求外部系统 |
| **连续失败熔断** | Trigger 连续 10 次状态查询失败触发熔断，停止轮询执行清理并主动取消远程 Ray 任务 |

## 安全属性分析

| 安全维度 | 设计与措施 |
|----------|-------------|
| **认证方式** | RayOperator 通过 Airflow Connection 存储的 Token 访问算力网关，与 SparkOperator 复用同一 Connection 类型 |
| **runtime_env 敏感数据** | 用户在 `runtime_env.env_vars` 中传入的环境变量随提交请求体落网关日志，提醒用户避免在该字段写入明文密钥（仅文档约定，不做拦截） |

## 隐私分析

不涉及，系统不收集个人信息。Ray 任务的输入输出数据由用户在 `entrypoint` 与 `runtime_env.working_dir` 中自主管理，Operator 不感知具体业务数据。

---

## US定义

| US编号 | US名称 | US描述 | 关联用例及场景 | 前后端协同契约 | 优先级 |
|--------|--------|--------|----------------|----------------|--------|
| US001 | 实现 Ray 任务提交核心逻辑 | 【As】作为一个 DAG 开发者<br>【I want】我想要在 DAG 脚本中引用 RayOperator 并提交 Ray 任务，系统自动完成参数校验和 OBS 路径转换<br>【so that】以便于无需手动处理协议转换即可使用本地挂载路径作为 Ray 工作目录<br><br>【given】DAG 中已配置 entrypoint、runtime_env、任务级 CPU/GPU/Memory 资源；Airflow Connection 已配置网关认证凭证<br>【When】DAG 调度执行到该 RayOperator 节点<br>【Then】若 entrypoint 为空或资源数值非法，任务标记错误并在日志记录字段级错误详情；若校验通过，从 Connection 读取凭证，结构化路径字段（runtime_env.working_dir）由 /mnt/OBS/... 自动转换为 OBS://...，向算力网关 POST /ray-job/v1/create 提交并获取 job_id 写入 XCom，触发 Deferrable 挂起协程 | UC001 (基本流程)、UC001 (分支 1a、2a、2b、2c) | **Operator 侧**：实现 RayOperator 继承 BaseAIDataLakeOperator，支持 OBS 结构化路径转换、参数校验、Connection 凭证读取、Ray 任务提交逻辑<br>**网关侧**：提供 POST /ray-job/v1/create 接口，请求体含 jobConfig.rayPyParameter（entrypoint/runtimeEnv/entrypointNumCpus/entrypointNumGpus/entrypointMemory） | P0 |
| US002 | 实现 Ray 任务异步状态轮询 | 【As】作为一个 DAG 开发者<br>【I want】我想要在任务提交后释放 Worker 资源，系统自动轮询 Ray 状态直到终态<br>【so that】以便于长时间运行的 ML 训练任务不阻塞 Worker 池<br><br>【given】任务已提交并获取 job_id；Trigger 进程正常运行<br>【When】Trigger 按 30s 间隔轮询 POST /ray-job/v1/query<br>【Then】每次轮询成功时更新 state 到 XCom；当返回 Ray 终态 SUCCEEDED 时触发回调恢复 Operator 并将 Airflow 任务标记 success；当返回 FAILED/STOPPED 时标记 failed；连续失败达 10 次时触发熔断主动取消远程作业并标记 failed | UC002 (基本流程)、UC002 (分支 1a、1b) | **Operator 侧**：实现 Deferrable 异步轮询机制、Ray 原生状态枚举映射逻辑、熔断清理逻辑<br>**网关侧**：提供 POST /ray-job/v1/query 接口，响应体含 state（PENDING/RUNNING/SUCCEEDED/FAILED/STOPPED） | P0 |
| US003 | 实现 Ray 任务取消功能 | 【As】作为一个 DAG 开发者<br>【I want】我想要通过 Airflow UI 取消正在运行的 Ray 任务<br>【so that】以便于避免无效任务继续消耗 GPU 等高价值算力资源<br><br>【given】任务处于 PENDING 或 RUNNING 状态；Airflow Connection 已配置网关认证凭证<br>【When】用户在 UI 标记任务失败触发 on_kill 钩子<br>【Then】系统向 POST /ray-job/v1/cancel 发送取消指令，Ray 任务状态直接落 STOPPED 终态，Airflow 任务状态更新为 failed；若任务已是终态，网关幂等处理返回成功，Operator 不报错仅记录日志；若网关 2 次重试仍未响应，强制刷新本地任务为失败状态 | UC003 (基本流程)、UC003 (分支 2a、2b) | **Operator 侧**：实现 on_kill 钩子取消逻辑、终态幂等判断、网关无响应兜底<br>**网关侧**：提供 POST /ray-job/v1/cancel 接口，对已终态任务支持幂等返回 | P0 |
